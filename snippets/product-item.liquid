<!-- product-item -->
<div class="{{ classes }} product-item">
    <a href="{{ product.url }}">

        {% for tag in product.tags %}
            {% assign _tag = tag | downcase %}
            {% if _tag contains "new" %}
                <span class="tag">НОВОЕ</span>
                {% break %}
            {% endif %}
        {% endfor %}

        <div class="top">
            {% assign productImage = product.featured_media | img_url: 'master' %}
            <img src="{{ productImage }}" alt="{{ product.title }}">
        </div>
        <div class="bottom" style="display: none;">
            <h4 class="title">{{ product.title }}</h4>
            <span class="price">{{ product.price | money }}</span>
            {{ product.description }}
        </div>
    </a>
    <button type="button" class="btn btn-product">больше</button>
</div>
<!-- product-item // -->